# Core dependencies — everything the simulator needs to run offline.
streamlit>=1.32
numpy>=1.26
scipy>=1.12
astropy>=6.0
plotly>=5.20
pandas>=2.2
numba>=0.59
h5py>=3.10
tqdm>=4.66

# Optional dependencies — NOT required to run the app.
# The repo bundles a pulsar catalogue snapshot (data/atnf_cache.json) and a
# precomputed DM grid (data/ne2001_grid.npz); these packages are only needed
# to regenerate that data from source models.
#
#   pip install psrqpy    # live ATNF catalogue refresh
#   pip install pygedm    # NE2001/YMW16 ISM model for DM-grid precomputation
#                         # (needs a C/Fortran toolchain incl. f2c to build)
